#include <stdio.h>
#include <stdlib.h>

int main() {
    // int *a = NULL;
    // int *a = (int *) malloc(sizeof(int));
    //
    // printf("%d\n", *a);

    // free(a);

    int a[100];
    a[100] = 0;

    return 0;
}